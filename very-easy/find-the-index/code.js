function find_index(arr, str) {
  // Your code here.
  // Arrayning indexOf metodi orqali berilgan satrning indexini olamiz
  return arr.indexOf(str) // va uning index raqamini return qilamiz
}

module.exports = find_index;
