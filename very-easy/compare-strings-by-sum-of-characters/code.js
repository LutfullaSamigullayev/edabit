function comp(str1, str2) {
  // Your code here.
  // Har ikkala stringning uzunligini solishtiramiz
  // Agar uzunliklar teng bo‘lsa, true qaytadi
  return str1.length === str2.length;
}

module.exports = comp;
